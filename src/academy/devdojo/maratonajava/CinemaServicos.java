/*package academy.devdojo.maratonajava;

public class CinemaServicos {
    public static boolean idadeValida(int escolhaFilme, int idade) {
        if (escolhaFilme == 1 && idade < 18) return false;
        return true;
    }

    public static int calcularTotal(int preco, int quantidade) {
        if (quantidade <= 0) throw new IllegalArgumentException("Quantidade deve ser maior que zero.");
        return preco * quantidade;
    }

    public static boolean formaPagamentoValida(String forma) {
        if (forma == null) return false;
        String f = forma.toLowerCase();
        return f.equals("débito") || f.equals("credito") || f.equals("pix");
    }
}
*/